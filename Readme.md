# LabStation Test Bench

LabStation Test Bench launches tests based on Selenium to a specific server. 

## Usage

Tests are defined in maven format. So you can execute them with:

```
mvn clean test
```

But first, update the application.properties to point to the correct server. 